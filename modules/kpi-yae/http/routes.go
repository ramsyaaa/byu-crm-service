package http

import (
	"github.com/gofiber/fiber/v2"
)

func KpiYaeRangeRoutes(router fiber.Router, handler *KpiYaeHandler) {
	// authRouter := router.Group("/kpi-yae",
	// 	middleware.JWTMiddleware,
	// 	middleware.JWTUserContextMiddleware(),
	// )
}
